using UnityEngine;
using System.Collections;

public class MainMenuMovingStars : MonoBehaviour {

	// Use this for initialization
	void Start () {
		transform.Rotate(Vector3.up * 50f);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
